apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-prestation-config
  namespace: default
data:
  postgres_user: prestation
  postgres_password: prestation-password
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-prestation-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-prestation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-prestation
  template:
    metadata:
      labels:
        app: postgres-prestation
    spec:
      volumes:
        - name: postgres-prestation-storage
          persistentVolumeClaim:
            claimName: postgres-prestation-pv-claim
      containers:
        - image: postgres:latest
          name: postgresp
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
              name: postgresp
          volumeMounts:
            - name: postgres-prestation-storage
              mountPath: /var/lib/postgresql/data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-prestation
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres-prestation